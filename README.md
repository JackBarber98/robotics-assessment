# How to Use

Launch the standard UoL RosPy maze launch file:
``` roslaunch uol_turtlebot_simulator maze2.launch ``` to initialise the environment.

Simply execute the command 
``` python maze_solver.py ``` to start navigating the maze with a virtual Turtlebot!